class cartItem {
    constructor(quantity, productTitle, productPrice, sum, imageUrl) {
        this.quantity = quantity;
        this.productTitle = productTitle;
        this.productPrice = productPrice;
        this.sum = sum;
        this.imageUrl = imageUrl;
    }
}

export default cartItem;